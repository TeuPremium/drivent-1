async function getAllHotels() {
  return 0;
}

async function getHotelById(id: number) {
  console.log(id);
  return 0;
}

const hotelServices = {
  getAllHotels,
  getHotelById,
};

export default hotelServices;
